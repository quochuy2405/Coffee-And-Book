import React from 'react';
import PropTypes from 'prop-types';
import Header from '../../components/Header';

Cart.propTypes = {
    
};

function Cart(props) {
    return (
        <div>
           <Header/>
        </div>
    );
}

export default Cart;