import React from 'react';
import Header from '../../components/Header';

Store.propTypes = {
    
};

function Store(props) {
    return (
        <div>
             <Header/>
        </div>
    );
}

export default Store;