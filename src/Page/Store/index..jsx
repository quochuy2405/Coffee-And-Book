import React from 'react';
import PropTypes from 'prop-types';

Store.propTypes = {
    
};

function Store(props) {
    return (
        <div>
            <p>Store</p>
        </div>
    );
}

export default Store;